# Point the dashboard at your deployed backend. Leave these UNSET for local dev
# (defaults to http://127.0.0.1:8000). Set them as build env vars on Vercel/Netlify.
VITE_API_URL=https://your-backend-host
VITE_WS_URL=wss://your-backend-host/ws/live
